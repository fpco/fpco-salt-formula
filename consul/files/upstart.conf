{%- if webui %}
{%- set cmd_args = cmd_args + ' -ui-dir /home/consul/webui/' %}
{%- endif -%}

description "{{ description }}"
author "devops@fpcomplete.com"
start on filesystem
stop on runlevel [!2345]
respawn
chdir {{ chdir }}
setuid {{ run_as }}
script
  exec {{ bin_path }} {{ cmd_args }} -config-file config.json
end script
